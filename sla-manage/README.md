# 🚀 X-UI SLA Manager (مدیریت هوشمند کاربران و ورودی‌ها)

**ابزاری پیشرفته، امن و خودکار برای مدیریت گروهی کاربران (Users) و ورودی‌ها (Inbounds) در پنل‌های X-UI**

![X-UI SLA Tool](xuislatool.png)

---

## 📖 معرفی

مدیریت دستی تاریخ انقضا و ترافیک برای تعداد زیادی کاربر یا پورت ورودی (Inbound) کاری زمان‌بر و خطاست. **X-UI SLA Manager** ابزاری مبتنی بر پایتون است که مستقیماً با دیتابیس تعامل می‌کند و به شما اجازه می‌دهد **سیاست‌های جدید (SLA)** را به صورت گروهی، درصدی یا ثابت اعمال کنید.

**🔥 ویژگی‌های جدید در نسخه اخیر:**
* پشتیبانی از تغییرات روی **Inboundها** (علاوه بر کاربران).
* **بکاپ‌گیری خودکار** از دیتابیس قبل از هر تغییر.
* تشخیص هوشمند اکانت‌های **نامحدود (Unlimited)** و عدم تغییر آن‌ها.

---

## ✨ ویژگی‌های کلیدی

* **🎯 انتخاب هدف (Target):** امکان اعمال تغییرات فقط روی `Users`، فقط `Inbounds` یا هر دو (`All`).
* **📦 بکاپ خودکار:** ساخت خودکار نسخه پشتیبان در پوشه `bkup/` قبل از هرگونه تغییر در دیتابیس.
* **🔍 فیلترینگ دقیق:** جستجو و تغییر بر اساس نام (Email) یا توضیحات (Remark).
* **🧮 محاسبات منعطف:** پشتیبانی از درصد (مثلاً `-20%` حجم) یا عدد ثابت (مثلاً `+10` روز).
* **📅 تقویم شمسی:** گزارش‌دهی دقیق با تاریخ خورشیدی.
* **♾️ هوشمندی:** اگر کاربری زمان انقضای نامحدود داشته باشد، سیستم آن را نادیده می‌گیرد (Skipped).

---

## 📥 نصب و اجرا

کافیست دستور زیر را در ترمینال سرور خود اجرا کنید:

```bash
bash <(curl -Ls [https://raw.githubusercontent.com/worldof01/x-ui/main/sla-manage/xuislatool.sh](https://raw.githubusercontent.com/worldof01/x-ui/main/sla-manage/xuislatool.sh))
