# 🚀 X-UI SLA Manager (مدیریت پیشرفته SLA کاربران و Inboundها)

**ابزاری قدرتمند، امن و سریع برای مدیریت هوشمند SLA در پنل X-UI**

![نمای اصلی برنامه](xuislatool.png)

---

## 📖 معرفی

مدیریت SLA (زمان انقضا و حجم ترافیک) برای تعداد زیادی **کاربر (Clients)** یا **Inbound** در پنل X-UI می‌تواند خسته‌کننده، زمان‌بر و پرخطا باشد.  
ابزار **X-UI SLA Manager** با استفاده از **Bash + Python** و اتصال مستقیم به دیتابیس **SQLite**، این فرآیند را به‌شکل **سریع، دقیق و ایمن** انجام می‌دهد.

با این ابزار می‌توانید SLA را برای:

- 👤 کاربران (Clients)
- 🔌 Inboundها
- 🌐 یا هر دو به‌صورت هم‌زمان

تنها در چند ثانیه مدیریت کنید.

---

## ✨ ویژگی‌های کلیدی

- **🎯 Target هوشمند:** انتخاب محدوده اعمال تغییرات (`users` / `inbounds` / `all`)
- **🔍 فیلترینگ پیشرفته:**
  - کاربران → بر اساس `email`
  - Inboundها → بر اساس `remark`
- **🧮 محاسبات منعطف:** تغییرات **درصدی (٪)** یا **عددی ثابت**
- **📅 تاریخ شمسی:** نمایش تاریخ انقضا به‌صورت **Jalali**
- **🛡️ بکاپ خودکار:** تهیه نسخه پشتیبان قبل از هر تغییر
- **📊 گزارش دقیق:** نمایش تغییرات قبل و بعد + فایل گزارش
- **🔄 ریستارت خودکار X-UI:** پس از اعمال موفق
- **🎨 رابط ترمینالی زیبا:** تم سایبرپانک با لوگوی اختصاصی

---

## 📥 نصب و اجرا (Installation)

برای اجرا، کافیست دستور زیر را در ترمینال سرور وارد کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/worldof01/x-ui/main/sla-manage/xuislatool.sh)
```

---

## 💡 راهنمای تصویری (نمونه واقعی)

![راهنمای استفاده](help.png)

تصویر بالا یک نمونه واقعی از اجرای اسکریپت و اعمال SLA روی کاربران و Inboundها را نشان می‌دهد.

---

## 🧪 سناریو نمونه (Practical Example)

### 🎯 هدف
- فقط کاربران
- فیلتر: کاربرانی که ایمیل آن‌ها شامل `test` است
- زمان انقضا: ۱۰ روز افزایش
- حجم ترافیک: ۲۰٪ کاهش

### ⚙️ تنظیم ورودی‌ها

```
Target      : users
Days SLA    : 10
Traffic SLA : -20%
Filter      : test
```

### ✅ نتیجه عملیات

- ➕ ۱۰ روز به تاریخ انقضا اضافه می‌شود
- ➖ ۲۰٪ از حجم ترافیک کم می‌شود
- 🎯 فقط کاربران شامل `test`
- ♻️ سرویس X-UI به‌صورت خودکار ریستارت می‌شود

---

## 🎯 راهنمای Target

| مقدار | توضیح |
|-------|-------|
| `users` | فقط کاربران (Clients داخل Inboundها) |
| `inbounds` | فقط Inboundها |
| `all` | کاربران و Inboundها (پیش‌فرض) |

---

## ⏳ Days SLA (زمان انقضا)

| ورودی | عملکرد |
|-------|--------|
| `10` | اضافه کردن ۱۰ روز |
| `-5` | کم کردن ۵ روز |
| `20%` | افزایش ۲۰٪ از زمان باقی‌مانده |
| `-10%` | کاهش ۱۰٪ |
| *(خالی)* | مقدار پیش‌فرض: ۱ روز |

> ⚠️ آیتم‌های Unlimited بدون تغییر باقی می‌مانند.

---

## 📦 Traffic SLA (حجم ترافیک)

| ورودی | عملکرد |
|-------|--------|
| `10` | اضافه کردن ۱۰ گیگابایت |
| `-5` | کم کردن ۵ گیگابایت |
| `25%` | افزایش ۲۵٪ |
| `-20%` | کاهش ۲۰٪ |
| `0` | بدون تغییر (پیش‌فرض) |

---

## 🔍 Filter (فیلتر هوشمند)

- **Users:** فیلتر روی `email`
- **Inbounds:** فیلتر روی `remark`
- **خالی:** اعمال تغییرات روی همه

### مثال‌ها

```
test
vip
reseller01
```

---

## 🛡️ بکاپ و امنیت

قبل از هر تغییری، از دیتابیس زیر بکاپ گرفته می‌شود:

```
/etc/x-ui/x-ui.db
```

**مسیر ذخیره بکاپ‌ها:**

```
bkup/YYYY-MM-DD_HH-MM-SS/x-ui.db
```

---

## 📑 گزارش خروجی

پس از اتمام عملیات:

- جدول تغییرات (قبل ⟶ بعد)
- نمایش تاریخ‌ها به‌صورت شمسی
- ذخیره گزارش در فایل زیر:

```
sla_update_report.txt
```

---

## ☕ حمایت (Donate)

اگر این ابزار در مدیریت سرور به شما کمک کرد، خوشحال می‌شوم با حمایت شما انگیزه توسعه بیشتر ایجاد شود ❤️

**TON Wallet (The Open Network):**

```
UQAykVgirxEyv8cgHAgpPGXwzUYFwviRZWS1QMGwx3KDHrsV
```

---

⭐ اگر پروژه برایتان مفید بود، با یک **Star** در GitHub از آن حمایت کنید
